tools:
  - name: create_level_sequence
    description: >-
      Create a new Level Sequence asset in the Content Browser.
      Returns the package path of the created sequence for use with
      other cinematic tools.
    source_file: scripts/create_level_sequence.py
    execution: sync
    affinity: main
    enforce_thread_affinity: false
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: true
    input_schema:
      type: object
      required: [sequence_name]
      properties:
        sequence_name:
          type: string
          minLength: 1
          description: Name for the new Level Sequence asset.
        package_path:
          type: string
          default: /Game/Cinematics
          description: Content Browser folder path (must start with /Game).
        frame_rate:
          type: number
          default: 30.0
          description: Frame rate for the sequence (FPS).
    next-tools:
      on-success: [unreal_cinematics__open_level_sequence, unreal_cinematics__add_actor_to_sequence]
      on-failure: [dcc_diagnostics__screenshot, dcc_diagnostics__audit_log]

  - name: open_level_sequence
    description: >-
      Open a Level Sequence asset in the Sequencer editor tab.
      The asset must exist under /Game.
    source_file: scripts/open_level_sequence.py
    execution: sync
    affinity: main
    enforce_thread_affinity: false
    timeout_hint_secs: 15
    read_only: false
    destructive: false
    idempotent: true
    input_schema:
      type: object
      required: [sequence_path]
      properties:
        sequence_path:
          type: string
          minLength: 1
          description: Package path to the Level Sequence (e.g. /Game/Cinematics/MySequence).
    next-tools:
      on-success: [unreal_cinematics__get_sequence_info]

  - name: add_actor_to_sequence
    description: >-
      Add an actor from the current level as a spawnable or possessable
      binding in a Level Sequence. The binding name defaults to the actor
      label.
    source_file: scripts/add_actor_to_sequence.py
    execution: sync
    affinity: main
    enforce_thread_affinity: false
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: true
    input_schema:
      type: object
      required: [sequence_path, actor_name]
      properties:
        sequence_path:
          type: string
          minLength: 1
          description: Package path to the Level Sequence.
        actor_name:
          type: string
          minLength: 1
          description: Label or name of the actor in the current level.
        binding_type:
          type: string
          enum: [spawnable, possessable]
          default: possessable
          description: Whether to create a spawnable (new copy) or possessable (reference existing) binding.
        binding_name:
          type: string
          description: Custom name for the binding; defaults to the actor label.
    next-tools:
      on-success: [unreal_cinematics__add_transform_keyframe]

  - name: add_transform_keyframe
    description: >-
      Add a transform keyframe (location, rotation, scale) for a bound
      actor at a specific time in the Level Sequence.
    source_file: scripts/add_transform_keyframe.py
    execution: sync
    affinity: main
    enforce_thread_affinity: false
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    input_schema:
      type: object
      required: [sequence_path, binding_name, time]
      properties:
        sequence_path:
          type: string
          minLength: 1
          description: Package path to the Level Sequence.
        binding_name:
          type: string
          minLength: 1
          description: Name of the actor binding in the sequence.
        time:
          type: number
          description: Time in seconds for the keyframe.
        location:
          type: array
          minItems: 3
          maxItems: 3
          items:
            type: number
          description: '[x, y, z] world location in Unreal units (cm).'
        rotation:
          type: array
          minItems: 3
          maxItems: 3
          items:
            type: number
          description: '[pitch, yaw, roll] rotation in degrees.'
        scale:
          type: array
          minItems: 3
          maxItems: 3
          items:
            type: number
          description: '[x, y, z] scale factor.'
    next-tools:
      on-failure: [dcc_diagnostics__screenshot]

  - name: set_playback_range
    description: >-
      Set the playback start and end times for a Level Sequence.
    source_file: scripts/set_playback_range.py
    execution: sync
    affinity: main
    enforce_thread_affinity: false
    timeout_hint_secs: 15
    read_only: false
    destructive: false
    idempotent: true
    input_schema:
      type: object
      required: [sequence_path, start_time, end_time]
      properties:
        sequence_path:
          type: string
          minLength: 1
          description: Package path to the Level Sequence.
        start_time:
          type: number
          default: 0.0
          description: Start time in seconds.
        end_time:
          type: number
          default: 5.0
          description: End time in seconds.
    next-tools:
      on-success: [unreal_cinematics__get_sequence_info]

  - name: get_sequence_info
    description: >-
      Return detailed information about a Level Sequence: playback range,
      tracks, bindings, and camera cuts.
    source_file: scripts/get_sequence_info.py
    execution: sync
    affinity: main
    enforce_thread_affinity: false
    timeout_hint_secs: 15
    read_only: true
    destructive: false
    idempotent: true
    input_schema:
      type: object
      required: [sequence_path]
      properties:
        sequence_path:
          type: string
          minLength: 1
          description: Package path to the Level Sequence.
    next-tools:
      on-success: [unreal_cinematics__add_camera_cut_track, unreal_cinematics__render_sequence_to_movie]

  - name: add_camera_cut_track
    description: >-
      Add a camera cut track to a Level Sequence and bind camera actors
      with time ranges for shot switching.
    source_file: scripts/add_camera_cut_track.py
    execution: sync
    affinity: main
    enforce_thread_affinity: false
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    input_schema:
      type: object
      required: [sequence_path, camera_name, start_time, end_time]
      properties:
        sequence_path:
          type: string
          minLength: 1
          description: Package path to the Level Sequence.
        camera_name:
          type: string
          minLength: 1
          description: Name of the CineCameraActor in the level.
        start_time:
          type: number
          description: Shot start time in seconds.
        end_time:
          type: number
          description: Shot end time in seconds.
        binding_name:
          type: string
          description: Custom name for the camera binding; defaults to camera_name.
    next-tools:
      on-success: [unreal_cinematics__get_sequence_info]

  - name: render_sequence_to_movie
    description: >-
      Queue a Level Sequence for cinematic rendering via the Movie Render Queue.
      Returns the job status. Not for real-time viewport capture.
    source_file: scripts/render_sequence_to_movie.py
    execution: async
    affinity: main
    enforce_thread_affinity: false
    timeout_hint_secs: 300
    read_only: false
    destructive: false
    idempotent: false
    input_schema:
      type: object
      required: [sequence_path, output_path]
      properties:
        sequence_path:
          type: string
          minLength: 1
          description: Package path to the Level Sequence.
        output_path:
          type: string
          minLength: 1
          description: Absolute output directory for rendered frames/video.
        resolution_x:
          type: integer
          default: 1920
        resolution_y:
          type: integer
          default: 1080
        output_format:
          type: string
          enum: [png, jpg, exr, avi]
          default: png
          description: Output file format.
        frame_rate_override:
          type: number
          description: Override frame rate; uses sequence default when omitted.
    next-tools:
      on-failure: [dcc_diagnostics__screenshot, dcc_diagnostics__audit_log]
